# IDEAS inbox — improving-research-os

> Low-cost capture. One line per idea. No full thread, no five-stage flow yet. Batch-triage during weekly meta-review.
>
> Format: `YYYY-MM-DD | source | one-sentence idea | (optional) hypothesis / next step`

---

## Backlog

| Date | Source | Idea | Status |
|------|--------|------|--------|
| 2026-04-26 | self | What if `projects/README.md` new-project guide had a 1-command scaffold that generates `projects/<name>/` from a Jinja template? | triage |
| 2026-04-26 | user feedback | The current Mermaid dark theme looks fine on GitHub but may be hard to read on small mobile screens. Check rendering on GitHub mobile app. | triage |
| 2026-04-26 | self | Should `frontmatter.json` (Front Matter VSCode extension config) have per-content-type schema for `wiki/papers/*` to enforce the YAML fields `doi` / `venue` / `year`? | triage |
| 2026-04-26 | self | Add a `superpowers-using-git-worktrees` upstream skill so researchers can run two experiments in parallel worktrees without conflicting `results/`. | triage |
| 2026-04-26 | user feedback | First-time users may not know that `CLAUDE.md §12` (Karpathy principles) is auto-loaded. Add a one-liner in README "What you get on `git clone`" to make this explicit. | promoted to thread `first-session-bootstrap` |

---

## How ideas leave this file

- **Promote**: an idea that merits a full five-stage investigation → create `tracks/<matching-track>/<new-thread>/` and mark the row `promoted to thread <slug>`
- **Archive**: the idea turned out to be wrong / duplicate / obsolete → mark `archived`
- **Park**: good but not now → mark `parked`

No idea deletes. History matters for future meta-review.
